@page "/counter"
@inject HttpClient Http

<PageTitle>Counter</PageTitle>

<MudText Typo="Typo.h3" GutterBottom="true">Counter</MudText>
<MudText Class="mb-4">Current count: @currentCount</MudText>
<MudButton Color="Color.Primary" Variant="Variant.Filled" @onclick="IncrementCount">Click me</MudButton>


@code {
    private int currentCount = 0;

    private void IncrementCount()
    {
        currentCount++;
    }

    //protected override async Task OnInitializedAsync()
    //{
    //    var movies = await Http.GetFromJsonAsync<DbMovie[]>("api/movie");
    //    var people = await Http.GetFromJsonAsync<DbPerson[]>("api/person");

    //    if (movies != null && people != null)
    //    {
    //        foreach (DbMovie m in movies)
    //        {
    //            foreach (DbPerson p in people)
    //            {
    //                if (m.Cast.Contains(p.FullName) || m.Directors.Contains(p.FullName))
    //                {
    //                    Console.WriteLine($"({m.Id}, {p.Id}),");
    //                }
    //            }
    //        }
    //    }
    //}
}
